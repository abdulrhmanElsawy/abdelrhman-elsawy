import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.portfolioabdelrhman.com',
  appName: 'portfolioabdelrhman',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
